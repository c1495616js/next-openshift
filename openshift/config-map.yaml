apiVersion: v1
kind: ConfigMap
metadata:
  name: nextjs-configmap
  namespace: fe64f4-dev # Specify your namespace here if needed
data:
  # Configuration settings as key-value pairs
  config.json: |
    {
      "key": "value"
    }
